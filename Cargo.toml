[package]
name = "rup"
version = "0.1.0"
edition = "2024"

[profile.release]
panic = 'abort'

[dependencies]
coloring = { git = "https://github.com/ntalbs/coloring.git" }

[dev-dependencies]
p-test = "1.0.2"
